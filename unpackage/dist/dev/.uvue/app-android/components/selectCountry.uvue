import { ref, computed } from 'vue';
// 选项数据类型
type SelectOption = UTSJSONObject;
// Props 定义
const __sfc__ = defineComponent({
    __name: 'selectCountry',
    props: {
        // 绑定的值
        modelValue: {
            type: [String, Number, null] as any,
            default: null
        },
        // 选项列表
        options: {
            type: Array as () => UTSJSONObject[],
            default: (): never[] => []
        },
        // 值字段名
        valueKey: {
            type: String,
            default: 'value'
        },
        // 标签字段名
        labelKey: {
            type: String,
            default: 'label'
        },
        // 搜索框占位符
        searchPlaceholder: {
            type: String,
            default: '请输入关键词搜索'
        },
        // 空状态文本
        emptyText: {
            type: String,
            default: '暂无数据'
        },
        // 最大高度
        maxHeight: {
            type: String,
            default: '600rpx'
        }
    },
    emits: ["update:modelValue", "change"],
    setup(__props, __setupCtx: SetupContext): () => any | null {
        const __expose = __setupCtx.expose;
        const __ins = getCurrentInstance()!;
        const _ctx = __ins.proxy as InstanceType<typeof __sfc__>;
        const _cache = __ins.renderCache;
        const props = __props;
        // Emits 定义
        function emit(event: string, ...do_not_transform_spread: Array<any | null>) {
            __ins.emit(event, ...do_not_transform_spread);
        }
        // 响应式数据
        const searchKeyword = ref<string>('');
        // 安全转换为字符串
        const safeToString = (value: any): string => {
            if (value == null)
                return '';
            if (typeof value === 'string')
                return value as string;
            if (typeof value === 'number')
                return (value as number).toString();
            return '';
        };
        // 获取选项标签
        const getItemLabel = (item: UTSJSONObject): string => {
            const label = item.getString(props.labelKey);
            if (label != null) {
                return label;
            }
            const num = item.getNumber(props.labelKey);
            if (num != null) {
                return num.toString(10);
            }
            return '';
        };
        // 获取选项值
        const getItemValue = (item: UTSJSONObject): string | number | null => {
            if (item == null)
                return null;
            const value = item[props.valueKey];
            if (value == null)
                return null;
            if (typeof value === 'string' || typeof value === 'number') {
                return value as any;
            }
            return safeToString(value);
        };
        // 手动实现过滤逻辑（避免使用 Array.filter）
        const getFilteredOptions = (): UTSJSONObject[] => {
            const optionsList = props.options as UTSJSONObject[];
            if (!(optionsList != null) || optionsList.length === 0) {
                return [];
            }
            const keyword = searchKeyword.value;
            if (!(keyword != "") || keyword.trim().length === 0) {
                return optionsList;
            }
            const lowerKeyword = keyword.toLowerCase();
            const result: UTSJSONObject[] = [];
            for (let i = 0; i < optionsList.length; i++) {
                const opt = optionsList[i];
                const label = getItemLabel(opt).toLowerCase();
                const itemValue = getItemValue(opt);
                let value = '';
                if (itemValue != null) {
                    value = safeToString(itemValue).toLowerCase();
                }
                if (label.includes(lowerKeyword) || value.includes(lowerKeyword)) {
                    result.push(opt);
                }
            }
            return result;
        };
        // 过滤后的选项列表
        const filteredOptions = computed((): UTSJSONObject[] => {
            return getFilteredOptions();
        });
        // 获取选项唯一key
        const getItemKey = (item: UTSJSONObject, index: number): string => {
            const value = getItemValue(item);
            if (value != null) {
                return safeToString(value);
            }
            return `option-${index}`;
        };
        // 判断是否选中
        const isSelected = (item: UTSJSONObject): boolean => {
            const currentValue = props.modelValue;
            const itemValue = getItemValue(item);
            if (currentValue == null)
                return false;
            if (itemValue == null)
                return false;
            return safeToString(currentValue) === safeToString(itemValue);
        };
        // 选择选项
        const selectOption = (item: UTSJSONObject) => {
            const newValue = getItemValue(item);
            emit('update:modelValue', newValue);
            emit('change', newValue, item);
        };
        // 处理搜索
        const handleSearch = () => {
            // 搜索逻辑已在 computed 中处理
        };
        // 清空搜索
        const clearSearch = () => {
            searchKeyword.value = '';
        };
        // 监听弹窗关闭时清空搜索（外部调用）
        const resetSearch = () => {
            searchKeyword.value = '';
        };
        // 暴露方法给父组件
        __expose({
            resetSearch
        });
        return (): any | null => {
            return _cE("view", _uM({ class: "search-select" }), [
                _cE("view", _uM({ class: "search-bar" }), [
                    _cE("view", _uM({ class: "search-input-wrapper" }), [
                        _cE("text", _uM({ class: "search-icon" }), "🔍"),
                        _cE("input", _uM({
                            modelValue: searchKeyword.value,
                            onInput: [($event: UniInputEvent) => { (searchKeyword).value = $event.detail.value; }, handleSearch] as Array<any | null>,
                            placeholder: _ctx.searchPlaceholder,
                            class: "search-input",
                            "confirm-type": "search"
                        }), null, 40 /* PROPS, NEED_HYDRATION */, ["modelValue", "onInput", "placeholder"]),
                        isTrue(searchKeyword.value)
                            ? _cE("text", _uM({
                                key: 0,
                                class: "clear-icon",
                                onClick: clearSearch
                            }), "✕")
                            : _cC("v-if", true)
                    ])
                ]),
                _cE("scroll-view", _uM({
                    class: "options-list",
                    "scroll-y": "",
                    enhanced: true,
                    "show-scrollbar": false,
                    style: _nS(_uM({ maxHeight: _ctx.maxHeight }))
                }), [
                    filteredOptions.value.length === 0
                        ? _cE("view", _uM({
                            key: 0,
                            class: "empty-state"
                        }), [
                            _cE("text", _uM({ class: "empty-text" }), _tD(_ctx.emptyText), 1 /* TEXT */)
                        ])
                        : _cC("v-if", true),
                    _cE(Fragment, null, RenderHelpers.renderList(filteredOptions.value, (item, index, __index, _cached): any => {
                        return _cE("view", _uM({
                            key: getItemKey(item, index),
                            class: _nC(["option-item", _uM({ 'is-selected': isSelected(item) })]),
                            onClick: () => { selectOption(item); }
                        }), [
                            _cE("text", _uM({ class: "option-label" }), _tD(getItemLabel(item)), 1 /* TEXT */),
                            isTrue(isSelected(item))
                                ? _cE("view", _uM({
                                    key: 0,
                                    class: "check-icon"
                                }), [
                                    _cE("text", _uM({ class: "icon-check" }), "✓")
                                ])
                                : _cC("v-if", true)
                        ], 10 /* CLASS, PROPS */, ["onClick"]);
                    }), 128 /* KEYED_FRAGMENT */)
                ], 4 /* STYLE */)
            ]);
        };
    }
});
export default __sfc__;
const GenComponentsSelectCountryStyles = [_uM([["search-select", _pS(_uM([["display", "flex"], ["flexDirection", "column"], ["width", "100%"], ["height", "100%"], ["backgroundColor", "#ffffff"]]))], ["search-bar", _pS(_uM([["paddingTop", "24rpx"], ["paddingRight", "32rpx"], ["paddingBottom", "24rpx"], ["paddingLeft", "32rpx"], ["borderBottomWidth", "2rpx"], ["borderBottomStyle", "solid"], ["borderBottomColor", "#f0f0f0"], ["flexShrink", 0]]))], ["search-input-wrapper", _uM([[".search-bar ", _uM([["display", "flex"], ["flexDirection", "row"], ["alignItems", "center"], ["backgroundColor", "#f5f7fa"], ["borderTopLeftRadius", "32rpx"], ["borderTopRightRadius", "32rpx"], ["borderBottomRightRadius", "32rpx"], ["borderBottomLeftRadius", "32rpx"], ["paddingTop", "16rpx"], ["paddingRight", "24rpx"], ["paddingBottom", "16rpx"], ["paddingLeft", "24rpx"]])]])], ["search-icon", _uM([[".search-bar .search-input-wrapper ", _uM([["fontSize", "28rpx"], ["color", "#999999"], ["marginRight", "16rpx"], ["flexShrink", 0]])]])], ["search-input", _uM([[".search-bar .search-input-wrapper ", _uM([["flexGrow", 1], ["flexShrink", 1], ["flexBasis", "0%"], ["fontSize", "28rpx"], ["color", "#333333"], ["backgroundImage", "none"], ["backgroundColor", "rgba(0,0,0,0)"], ["minHeight", "40rpx"]])]])], ["clear-icon", _uM([[".search-bar .search-input-wrapper ", _uM([["fontSize", "28rpx"], ["color", "#999999"], ["paddingTop", "8rpx"], ["paddingRight", "8rpx"], ["paddingBottom", "8rpx"], ["paddingLeft", "8rpx"], ["flexShrink", 0], ["lineHeight", 1]])]])], ["options-list", _pS(_uM([["flexGrow", 1], ["flexShrink", 1], ["flexBasis", "0%"], ["width", "100%"]]))], ["option-item", _uM([["", _uM([["display", "flex"], ["flexDirection", "row"], ["alignItems", "center"], ["justifyContent", "space-between"], ["paddingTop", "28rpx"], ["paddingRight", "32rpx"], ["paddingBottom", "28rpx"], ["paddingLeft", "32rpx"], ["borderBottomWidth", "2rpx"], ["borderBottomStyle", "solid"], ["borderBottomColor", "#f5f5f5"], ["backgroundColor", "#ffffff"], ["minHeight", "88rpx"], ["boxSizing", "border-box"], ["backgroundColor:active", "#f5f7fa"]])], [".is-selected", _uM([["backgroundColor", "#eff6ff"]])]])], ["option-label", _uM([[".option-item.is-selected ", _uM([["color", "#2563eb"]])], [".option-item ", _uM([["flexGrow", 1], ["flexShrink", 1], ["flexBasis", "0%"], ["fontSize", "28rpx"], ["color", "#333333"], ["lineHeight", 1.4], ["wordBreak", "break-all"]])]])], ["check-icon", _uM([[".option-item ", _uM([["width", "48rpx"], ["height", "48rpx"], ["display", "flex"], ["flexDirection", "row"], ["alignItems", "center"], ["justifyContent", "center"], ["flexShrink", 0], ["marginLeft", "16rpx"]])]])], ["icon-check", _uM([[".option-item .check-icon ", _uM([["fontSize", "32rpx"], ["color", "#2563eb"], ["fontWeight", "bold"], ["lineHeight", 1]])]])], ["empty-state", _pS(_uM([["paddingTop", "120rpx"], ["paddingRight", 0], ["paddingBottom", "120rpx"], ["paddingLeft", 0], ["display", "flex"], ["flexDirection", "row"], ["alignItems", "center"], ["justifyContent", "center"]]))], ["empty-text", _uM([[".empty-state ", _uM([["fontSize", "28rpx"], ["color", "#999999"], ["textAlign", "center"]])]])]])];
//# sourceMappingURL=selectCountry.uvue.map